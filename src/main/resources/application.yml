eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
server:
  port: 8761
spring:
  application:
    name: currency-exchange-microservices-naming-server
  cloud:
    config:
      enabled: false
